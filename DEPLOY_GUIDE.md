# Полный гайд по деплою проекта на Vercel

## 📋 Что нужно сделать:

1. ✅ Зарегистрироваться на Vercel
2. ✅ Создать базу данных на Neon (PostgreSQL)
3. ✅ Подключить GitHub репозиторий
4. ✅ Настроить переменные окружения
5. ✅ Задеплоить проект
6. ✅ Настроить webhook для Telegram бота

---

## Шаг 1: Регистрация на Vercel

### 1.1 Создай аккаунт

1. Открой: https://vercel.com/signup
2. Нажми **"Continue with GitHub"**
3. Авторизуйся через GitHub (или зарегистрируйся)
4. Готово! У тебя есть аккаунт

---

## Шаг 2: Создать базу данных на Neon

### 2.1 Регистрация на Neon

1. Открой: https://neon.tech
2. Нажми **"Sign Up"** → **"Continue with GitHub"**
3. Авторизуйся

### 2.2 Создать проект

1. Нажми **"Create a project"**
2. Заполни:
   - **Project name**: `tysha-cottages-db` (или любое имя)
   - **Region**: выбери ближайший (например: `Europe (Frankfurt)`)
   - **PostgreSQL version**: оставь последнюю
3. Нажми **"Create project"**

### 2.3 Получить connection string

После создания проекта увидишь:

```
postgresql://username:password@ep-xxxx-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

**✅ СКОПИРУЙ ЭТУ СТРОКУ** - это твой `DATABASE_URL`

**⚠️ ВАЖНО:** Добавь в конец `?sslmode=require` если его нет

---

## Шаг 3: Загрузить проект на GitHub

### 3.1 Если у тебя ещё нет репозитория:

```bash
# 1. Инициализируй git (если не сделано)
git init

# 2. Добавь все файлы
git add .

# 3. Сделай первый коммит
git commit -m "Initial commit"

# 4. Создай репозиторий на GitHub
# Открой: https://github.com/new
# Назови: tysha-cottages
# НЕ добавляй README, .gitignore (у тебя уже есть)
# Нажми "Create repository"

# 5. Привяжи удалённый репозиторий
git remote add origin https://github.com/ТВОЙ_USERNAME/tysha-cottages.git

# 6. Запуш файлы
git branch -M main
git push -u origin main
```

### 3.2 Если репозиторий уже есть:

```bash
git add .
git commit -m "Prepare for deployment"
git push
```

---

## Шаг 4: Деплой на Vercel

### 4.1 Импортировать проект

1. Открой: https://vercel.com/new
2. Нажми **"Import Git Repository"**
3. Найди репозиторий `tysha-cottages` → нажми **"Import"**

### 4.2 Настроить Build Settings

Vercel должен автоматически определить Next.js проект:

- **Framework Preset**: Next.js
- **Build Command**: `npm run build`
- **Output Directory**: `.next`
- **Install Command**: `npm install`

Оставь всё как есть.

### 4.3 Добавить Environment Variables (переменные окружения)

**⚠️ САМОЕ ВАЖНОЕ!** Нажми **"Environment Variables"** и добавь:

| Name | Value | Где взять |
|------|-------|-----------|
| `DATABASE_URL` | `postgresql://...` | Скопировал из Neon (Шаг 2.3) |
| `NEXTAUTH_URL` | `https://tysha-cottages.vercel.app` | Vercel даст URL после деплоя (сначала оставь пустым) |
| `NEXTAUTH_SECRET` | `случайная_строка_32+_символов` | Сгенерируй ниже ↓ |
| `ADMIN_KEY` | `твой_секретный_ключ` | Придумай сложный пароль |
| `TELEGRAM_BOT_TOKEN` | `8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M` | Твой токен бота |
| `TELEGRAM_BOT_USERNAME` | `Tyshacottagesbot` | Username бота (без @) |
| `TELEGRAM_ADMIN_ID` | `5188263497` | Твой Telegram chat ID |
| `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` | `Tyshacottagesbot` | Для клиента |

#### Генерация NEXTAUTH_SECRET:

**Вариант 1:** В терминале:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Вариант 2:** Онлайн:
https://generate-secret.vercel.app/32

Скопируй результат.

### 4.4 Деплой

1. Проверь что все переменные добавлены
2. Нажми **"Deploy"**
3. Жди 2-3 минуты
4. Готово! 🎉

### 4.5 Получить продакшн URL

После деплоя увидишь:
```
🎉 Your project is ready!
https://tysha-cottages.vercel.app
```

**✅ СКОПИРУЙ ЭТОТ URL**

---

## Шаг 5: Обновить NEXTAUTH_URL

1. На странице проекта в Vercel → **"Settings"** → **"Environment Variables"**
2. Найди `NEXTAUTH_URL`
3. Нажми **"Edit"**
4. Вставь скопированный URL: `https://tysha-cottages.vercel.app`
5. Нажми **"Save"**
6. Нажми **"Redeploy"** (чтобы применить изменения)

---

## Шаг 6: Настроить базу данных

### 6.1 Запустить миграции Prisma

В терминале на локальном компьютере:

```bash
# Установи переменную окружения с продакшн БД
$env:DATABASE_URL="postgresql://username:password@ep-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# Или на Mac/Linux:
export DATABASE_URL="postgresql://username:password@ep-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# Запусти миграцию
npx prisma migrate deploy

# Сгенерируй Prisma Client
npx prisma generate
```

Если миграций нет, создай их:
```bash
npx prisma migrate dev --name init
```

---

## Шаг 7: Настроить Telegram бота (webhook)

### 7.1 Удалить старый webhook (если был)

```bash
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/deleteWebhook"
```

### 7.2 Установить новый webhook с продакшн URL

Замени `https://tysha-cottages.vercel.app` на **СВОЙ URL** из Vercel:

```bash
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/setWebhook?url=https://tysha-cottages.vercel.app/api/telegram/webhook"
```

Должен вернуться:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

### 7.3 Проверить webhook

```bash
curl "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/getWebhookInfo"
```

Должно быть:
```json
{
  "ok": true,
  "result": {
    "url": "https://tysha-cottages.vercel.app/api/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

---

## Шаг 8: Протестировать всё

### 8.1 Проверить сайт

1. Открой: `https://твой-домен.vercel.app`
2. Должна открыться главная страница
3. Попробуй создать бронь

### 8.2 Проверить бота

1. Открой: https://t.me/Tyshacottagesbot
2. Напиши `/start`
3. Должна появиться кнопка **"📱 Поділитися номером"**
4. Авторизуйся
5. Попробуй команды: `/my_bookings`, `/contact`

### 8.3 Проверить админку

1. Создай бронь на сайте
2. Админу должно прийти уведомление в Telegram
3. Попробуй подтвердить/отклонить бронь через inline кнопки

---

## ✅ Готово! Проект работает 24/7

Теперь:
- ✅ Сайт доступен по URL
- ✅ База данных в облаке
- ✅ Telegram бот работает 24/7
- ✅ Админ получает уведомления
- ✅ Клиенты могут бронировать и отправлять чеки

---

## 🔄 Как обновлять проект

### Внести изменения в код:

```bash
# 1. Измени файлы
vim src/app/...

# 2. Закоммить
git add .
git commit -m "Update booking form"

# 3. Запушить
git push

# 4. Vercel автоматически задеплоит за 1-2 минуты!
```

**⚠️ Webhook трогать НЕ НУЖНО** - он остаётся тем же!

### Если нужно изменить переменные окружения:

1. Vercel → Settings → Environment Variables
2. Измени нужные переменные
3. Нажми "Redeploy"

---

## 🐛 Решение проблем

### Сайт не открывается

1. Проверь статус деплоя: Vercel → Deployments
2. Посмотри логи: нажми на деплой → "Logs"

### База данных не работает

1. Проверь что `DATABASE_URL` правильный в Vercel
2. Убедись что миграции были запущены (Шаг 6.1)

### Бот не отвечает

1. Проверь webhook:
   ```bash
   curl "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/getWebhookInfo"
   ```
2. Если URL неправильный → установи заново (Шаг 7.2)
3. Проверь что переменные `TELEGRAM_*` добавлены в Vercel

### "Module not found" ошибки

Убедись что все зависимости в `package.json`:
```bash
npm install
git add package.json package-lock.json
git commit -m "Update dependencies"
git push
```

---

## 📚 Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Neon Console: https://console.neon.tech
- Telegram Bot API: https://core.telegram.org/bots/api
- Prisma Docs: https://www.prisma.io/docs

---

## 🎉 Поздравляю!

Твой проект теперь в продакшне! Можешь делиться ссылкой с клиентами.
