# Локальная настройка Telegram бота (ngrok)

## Зачем это нужно?

Telegram бот работает через **webhook** - когда пользователь пишет боту, Telegram отправляет это на твой сервер. Но localhost недоступен из интернета, поэтому используем **ngrok** - он создает публичный URL который перенаправляет запросы на твой localhost.

---

## Пошаговая инструкция

### Шаг 1: Запусти сервер Next.js

В первом терминале:
```bash
npm run dev
```

Дождись сообщения: `✓ Ready on http://localhost:3000`

---

### Шаг 2: Запусти ngrok

**Вариант A: Через созданный скрипт**

Запусти файл: `setup-telegram-local.bat`

**Вариант B: Вручную**

Во втором терминале:
```bash
ngrok http 3000
```

Ты увидишь что-то вроде:
```
Session Status                online
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:3000
```

✅ **Скопируй этот URL** (например: `https://abc123.ngrok-free.app`)

---

### Шаг 3: Установи webhook

**Вариант A: Через скрипт**

1. Запусти файл: `set-webhook.bat`
2. Вставь скопированный URL из ngrok
3. Готово!

**Вариант B: Вручную**

Замени `YOUR_NGROK_URL` на скопированный URL:
```bash
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/setWebhook?url=YOUR_NGROK_URL/api/telegram/webhook"
```

Пример:
```bash
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/setWebhook?url=https://abc123.ngrok-free.app/api/telegram/webhook"
```

Должен вернуться:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

---

### Шаг 4: Протестируй бота

1. Открой бота: https://t.me/Tyshacottagesbot
2. Напиши `/start`
3. Должна появиться кнопка **"📱 Поділитися номером"**
4. Нажми на кнопку → авторизуйся
5. Попробуй команды: `/my_bookings`, `/contact`

---

## Важные моменты

### ⚠️ Каждый раз при перезапуске ngrok

Когда ты **закрываешь и снова запускаешь ngrok**, URL **меняется**:
```
Раньше: https://abc123.ngrok-free.app
Сейчас: https://xyz789.ngrok-free.app  ← НОВЫЙ URL
```

Поэтому нужно **заново установить webhook** с новым URL!

### 💡 Решение: Не закрывай ngrok

Пока тестируешь бота - оставь окно ngrok открытым. Можешь перезапускать `npm run dev` - ngrok работает отдельно.

### ✅ Правильный workflow:

1. Запусти `npm run dev` (терминал 1)
2. Запусти `ngrok http 3000` (терминал 2) - **не закрывай**
3. Установи webhook один раз
4. Тестируй бота
5. Вносишь правки в код → сохраняешь → Next.js перезагружается автоматически
6. Продолжаешь тестировать (webhook всё ещё работает)

---

## Когда закончишь тестирование

### Остановка webhook

Запусти файл: `stop-webhook.bat`

Или вручную:
```bash
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/deleteWebhook"
```

Это удалит webhook, чтобы Telegram не пытался отправлять запросы на старый ngrok URL.

---

## Проверка статуса webhook

В любой момент можешь проверить установлен ли webhook:

Запусти файл: `check-webhook.bat`

Или вручную:
```bash
curl "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/getWebhookInfo"
```

Ответ покажет:
- **url**: Текущий webhook URL (или пусто если не установлен)
- **pending_update_count**: Сколько сообщений ждут обработки
- **last_error_date**: Когда была последняя ошибка

---

## Типичные проблемы

### Бот не отвечает

1. ✅ Проверь что `npm run dev` запущен
2. ✅ Проверь что ngrok запущен (окно не закрыто)
3. ✅ Проверь webhook: запусти `check-webhook.bat`
   - Если URL пустой → установи webhook заново
   - Если URL старый (ngrok был перезапущен) → установи webhook с новым URL

### "Webhook is already set"

Это нормально - значит webhook уже установлен. Чтобы обновить:
```bash
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/setWebhook?url=NEW_URL/api/telegram/webhook"
```

### "Bad Request: wrong HTTP URL"

URL должен быть **https** (не http). Ngrok автоматически создает https URL.

---

## Переход на продакшн (хостинг)

Когда будешь готов задеплоить на Vercel/Railway:

1. **Удали локальный webhook**:
   ```bash
   curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/deleteWebhook"
   ```

2. **Задеплой проект**:
   ```bash
   vercel
   ```

3. **Установи webhook с продакшн URL**:
   ```bash
   curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/setWebhook?url=https://your-domain.vercel.app/api/telegram/webhook"
   ```

---

## Команды для быстрого доступа

Все команды в одном месте:

```bash
# Запустить сервер
npm run dev

# Запустить ngrok
ngrok http 3000

# Установить webhook (замени URL)
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/setWebhook?url=YOUR_NGROK_URL/api/telegram/webhook"

# Проверить webhook
curl "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/getWebhookInfo"

# Удалить webhook
curl -X POST "https://api.telegram.org/bot8507685612:AAFaeLAJ2T7i4NtxV-Bs153Kz_1qPTcfZ2M/deleteWebhook"
```

---

## Что дальше?

После настройки webhook протестируй полный flow:

1. ✅ Админ получает уведомления о новых бронях
2. ✅ Клиент может авторизоваться в боте по телефону
3. ✅ Клиент может отправлять чеки оплаты
4. ✅ Админ может подтверждать/отклонять брони через inline кнопки
5. ✅ Клиент получает уведомления о статусе брони

Если всё работает - можно деплоить!
